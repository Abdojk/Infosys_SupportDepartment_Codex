/*
  PLACEHOLDER FILE - REQUIRED ACTION

  This project expects the official @azure/msal-browser browser bundle at:
    /vendor/msal-browser.min.js

  Download the official minified browser bundle for your approved MSAL browser version
  from Microsoft's official package distribution (npm tarball / package contents), then
  copy the real file here and replace this placeholder.

  Expected global symbol used by this app:
    window.msal

  If this placeholder remains, authentication will fail intentionally.
*/

(function () {
  if (!window.msal) {
    console.error(
      'MSAL bundle is missing. Replace /vendor/msal-browser.min.js placeholder with the official msal-browser minified browser bundle.'
    );
  }
})();
